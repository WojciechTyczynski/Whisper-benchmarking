device: cuda
model: tiny
input: ./data
output: ./transcribtion
available_models: [tiny.en, tiny, base, base.en, base, smalle.en, small, medium.en, medium, large]
greet_message: "Hello AI "
decode_options:
  # language: English
  # task: transcribe
  temperature: 0.5
  best_of: 10
  fp16: true
  verbose: false