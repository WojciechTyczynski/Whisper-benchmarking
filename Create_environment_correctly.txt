Setup environment

# install torch like this 
pip3 install torch==0.10.1 torchvision==0.11.1 torchaudio==0.10.1 --extra-index-url https://download.pytorch.org/whl/cu113

# install other dependencies
pip3 install -r requirements.txt

Modules:
module load cuda/11.3
module load ffmpeg
